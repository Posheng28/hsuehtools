# Guest JWT for CMoney holdings API. Capture from F12 on https://www.cmoney.tw/etf/tw/<ticker>/fundholding,
# look at the Authorization header of the POST to /api/customReport/app/v2/dtno/JsonCsv.
# Valid ~24h; refresh when /api/fund-crawl returns 401.
CMONEY_GUEST_TOKEN=
